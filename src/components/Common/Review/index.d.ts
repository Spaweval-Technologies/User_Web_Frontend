type ReviewProps = {
    starCount: number,
    rating: number,
    reviews: number,
    show?: boolean,
    iconHeight?: number,
    iconWidth?: number,
    mobileWidth?: number,
    mobileHeight?: number,
}

export default ReviewProps;